#include <stdio.h>

int main(argc){


type_prompt(); /* display pormpt on the screen */
read_command(command); /* read input from terminal */

printf("Number of item in the command line: %d\n", argc);

return 0;
}
